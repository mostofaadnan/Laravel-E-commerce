<!-- <table class="table table-striped data-table" id="invoicetable"> -->
<style>

</style>
<table class="table table-striped table-bordered table-sm data-table" cellspacing="0" id="table" >
    <thead>
        <tr>
            <th width="5%"> #@lang('home.sl') </th>
          <!--   <th> @lang('home.item') @lang('home.code') </th> -->
            <th> @lang('home.description')</th>
            <th width="10%"> @lang('home.quantity') </th>
            <th width="10%"> @lang('home.unit') @lang('home.price') </th>
            <th width="10%"> @lang('home.amount') </th>
         <!--    <th> @lang('home.discount') </th>
            <th> @lang('home.vat') </th>
            <th> @lang('home.nettotal')</th> -->
            <th width="10%"> @lang('home.action')</th>
        </tr>
    </thead>
    <tbody id="datatablebody" >

    </tbody>
   <!--  <tfoot>
        <tr>
            <th> #@lang('home.sl') </th>
            <th> @lang('home.item') @lang('home.code') </th>
            <th> @lang('home.description')</th>
            <th> @lang('home.quantity') </th>
            <th> @lang('home.unit') @lang('home.price') </th>
            <th id='amounts'> @lang('home.amount') </th>
            <th> @lang('home.discount') </th>
            <th> @lang('home.vat') </th>
            <th> @lang('home.nettotal')</th>
            <th> @lang('home.action')</th>
        </tr>
    </tfoot> -->
</table>